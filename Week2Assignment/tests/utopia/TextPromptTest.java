package utopia;

import static org.junit.Assert.assertEquals;


import org.junit.Test;


public class TextPromptTest {

	@Test
	public void getIntInputTest() {
		
		// Just a simple test to get it setup
		// One thing to thing about in the future will be
		// Writing programs around these unit tests. That means using less void methods
		// and instead having clearly defined input parameters that allow for easy unit testing.
		// Although these more ambiguous functions might still be testable with integration tests
		// or end-to-end tests when we get around to learning those.
		assertEquals(1, 1);

	}

}
